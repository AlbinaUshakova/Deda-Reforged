Optional printed (per-letter) images.

Path: /public/print/mkhedruli/LETTER.svg
If you add images here and set preferPrintedImages=true on DualScriptGeorgian,
the printed mode will render via these images instead of a font.
